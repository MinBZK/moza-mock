meta {
  name: Code verifieren
  type: http
  seq: 2
}

post {
  url: {{verificatieUrl}}/verify
  body: json
  auth: none
}

body:json {
  {
    "referenceId": "{{referenceId}}",
    "code": "123456"
  }
}

assert {
  res.status: eq 200
}
