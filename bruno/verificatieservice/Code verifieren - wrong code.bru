meta {
  name: Code verifieren - wrong code
  type: http
  seq: 3
}

post {
  url: {{verificatieUrl}}/verify
  body: json
  auth: none
}

body:json {
  {
    "referenceId": "{{referenceId}}",
    "code": "000000"
  }
}

assert {
  res.status: eq 200
}
